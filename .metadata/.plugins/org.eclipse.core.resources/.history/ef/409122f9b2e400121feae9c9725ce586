package groovy_metadata_registry

class RESTAPIFilters {

    def filters = {
        testREST(uri:'/api/**') {
            before = {
				println 'testing rest filter'

            }
            after = { Map model ->

            }
            afterView = { Exception e ->

            }
        }
    }
}
