package uk.co.mdc.pathwayEditor

import geb.spock.GebReportingSpec

/**
 * Created by rb on 17/01/2014.
 */
class PathwayCreationSpec extends GebReportingSpec{
    //When: a pathway is created
    //Then: a single node should be created and added to the canvas
    //And: the node should be selected and the properties visible
}
